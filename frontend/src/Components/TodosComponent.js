

export function TodosComponent(props) {
    return(
        <>
            <div>Afg: {props.todo.aufgabe}</div>
            <div>Beschreibung: {props.todo.beschreibung}</div>
           
        </>
        )
}